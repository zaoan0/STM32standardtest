# STM32 直线循迹小车

这是我学习 STM32 嵌入式开发过程中做的一个直线循迹小车项目，基于 STM32F103C8 标准外设库，用5路灰度传感器检测黑线，双电机 PWM 控制转向。代码比较基础，记录了从简单到逐步完善的过程。

v1.1.0 新增 MPU6050 陀螺仪，用于脱线恢复时的方向控制。

---

## 硬件配置

| 组件 | 说明 |
|------|------|
| MCU | STM32F103C8 |
| 传感器 | 5路灰度传感器（GPIOB: Pin 3/4/5/12/13） |
| 电机驱动 | 双电机 PWM 控制 |
| 陀螺仪 | MPU6050（软件I2C: PB8-SCL, PB9-SDA） |
| 串口 | USART1（PA9-TX，PA10-RX）115200bps |

---

## 项目结构

```
STM32standardtest/
├── CORE/                        # ARM Cortex-M3 核心文件
├── FWLIB/                       # STM32 标准外设库
├── USER/
│   ├── main.c                   # 主程序（当前版本 v1.1.0）
│   ├── main_v1.0.0.c            # 历史版本 v1.0.0 代码存档
│   ├── motor.c/h                # 电机初始化和 PWM 控制
│   ├── grayscale.c/h            # 灰度传感器读取
│   ├── mpu6050.c/h              # MPU6050 软件I2C驱动（v1.1.0 新增）
│   ├── pid.c/h                  # PID 算法（预留，暂未启用）
│   ├── encoder.c/h              # 编码器读取（预留）
│   ├── debug.c/h                # 串口调试（预留）
│   ├── delay.c/h                # 延时函数（DWT微秒 + SysTick毫秒）
│   └── trace.c/h                # 示踪引脚控制（预留）
├── .gitignore
└── stm32standardtest.uvprojx   # Keil 工程文件
```

---

## 引脚分配

| 引脚 | 功能 | 模块 |
|------|------|------|
| PA6 | 左轮 PWM | motor (TIM3_CH1) |
| PA7 | 右轮 PWM | motor (TIM3_CH2) |
| PA9 | USART1 TX | debug |
| PA10 | USART1 RX | debug |
| PB0 | 左轮 IN1 | motor |
| PB1 | 左轮 IN2 | motor |
| PB3 | 灰度 OUT1（最右） | grayscale |
| PB4 | 灰度 OUT2（右中） | grayscale |
| PB5 | 灰度 OUT3（中间） | grayscale |
| PB8 | MPU6050 SCL | mpu6050（软件I2C） |
| PB9 | MPU6050 SDA | mpu6050（软件I2C） |
| PB10 | 右轮 IN3 | motor |
| PB11 | 右轮 IN4 | motor |
| PB12 | 灰度 OUT4（左中） | grayscale |
| PB13 | 灰度 OUT5（最左） | grayscale |
| PB14 | 电机 STBY | motor |

---

## 版本历史

### v1.1.0（2026-05-01）— 当前版本 ✅

在 v1.0.1 基础上新增 **MPU6050 陀螺仪辅助脱线恢复**，脱线后根据传感器方向使用陀螺仪控制转向角度主动寻线。

**相比 v1.0.1 新增的内容：**
- 新增 `mpu6050.c/h` 软件 I2C 驱动（PB8-SCL, PB9-SDA）
- 新增 DWT 微秒延时（`delay_us`），基于 Cortex-M3 周期计数器
- 脱线恢复：记录 yaw 快照，按传感器方向转固定角度（最大30°）找线
- 选择性积分：角速度 > 1 dps 时才积分 yaw，静止时不漂移
- 启动校准：上电静止 1.25 秒，采 500 次取平均作为零偏

**脱线恢复逻辑：**
```
灰度传感器检测到线 → 正常循迹（差速转向）
    ↓
全部传感器脱线
    ↓
判断上次传感器方向：
  - SL/LM = 1 → 左转最多 30° 找线
  - SR/RM = 1 → 右转最多 30° 找线
  - 无方向信息 → 保持直行
    ↓
陀螺仪实时积分 yaw 角，P 控制器平滑转向目标角度
    ↓
灰度传感器重新检测到线 → 恢复正常循迹
```

**参数配置：**
```c
#define SPD_BASE        200   // 基础速度
#define SPD_SLIGHT       75   // 微调转向速度差
#define SPD_STRONG      150   // 强转向速度差
#define GYRO_SCALE    0.01527f // 陀螺仪 LSB→dps (1/65.5)
#define INTEG_THRESH    1.0f   // 积分角速度门限 (度/秒)
#define LOST_ANGLE     30.0f   // 脱线最大转向角度 (度)
#define LOST_KP         4.0f   // 脱线转向比例系数
```

**已知问题：**
- ⚠️ 长时间旋转仍会有积分漂移（MPU6050 固有限制）
- ⚠️ 需要 MPU6050 模块，SCL/SDA 需 4.7k 上拉电阻（部分模块自带上拉）

---

### v1.0.1（2026-04-26）— 历史版本

在 v1.0.0 基础上新增了**脱线状态标志位**和**稳定计数机制**，让脱线后的寻线恢复判断更可靠。

**相比 v1.0.0 新增的内容：**
- 新增 `lost_flag` 标志位，脱线时进入独立的恢复模式，正常循迹和脱线处理逻辑完全分离
- 新增 `stable_cnt` 稳定计数，传感器重新检测到线后需连续稳定 `STABLE_COUNT` 次才算真正找回，防止偶发抖动误判为找到线
- 脱线期间持续保持上次转向速度运动，主动向线的方向靠近

**脱线恢复逻辑：**
```
全部传感器脱线
    ↓
lost_flag = 1，保持上次左右速度继续运动
    ↓
仅有1个传感器检测到线 → stable_cnt 开始累加
    ↓
stable_cnt >= STABLE_COUNT（连续稳定）→ lost_flag = 0，恢复正常循迹
    ↓
期间传感器数量变化 → stable_cnt 清零重新计数
```

**参数配置：**
```c
#define SPD_BASE      200   // 基础速度
#define SPD_SLIGHT     75   // 微调转向速度差
#define SPD_STRONG    150   // 强转向速度差
#define STABLE_COUNT   15   // 恢复所需稳定帧数（约0.5s，可调小加快响应）
```

**已知问题：**
- ⚠️ 脱线后有时会一直转圈找不到线，即使小车已经回到线上，`stable_cnt` 仍然无法累积到 `STABLE_COUNT`
- 原因初步判断：判断条件为「仅1个传感器检测到线」过于严格，小车回归时可能同时触发2个传感器，导致条件不满足、计数一直被清零
- 后续计划放宽判断条件（如改为「至少1个传感器检测到线」），或降低 `STABLE_COUNT` 数值

---

### v1.0.0（2026-04-26）— 历史版本

最初版本，循迹逻辑和脱线处理写在同一个 while 循环里，结构简单，便于理解。通过 `LOST_MODE` 宏在编译时切换两种脱线策略。

**脱线处理方式：**
```c
#define LOST_MODE   0   // 模式0：保持上次转向速度继续寻线（推荐）
#define LOST_MODE   1   // 模式1：直走，等待重新碰到线
```

**参数配置：**
```c
#define SPD_BASE    200
#define SPD_SLIGHT   75
#define SPD_STRONG  150
#define LOST_MODE     0
```

**特点：**
- ✅ 代码量少，逻辑直观，适合入门学习
- ✅ 两种脱线策略一键切换，方便调试对比
- ⚠️ 没有独立的脱线状态，脱线处理和正常循迹混在一起
- ⚠️ 无稳定计数，传感器抖动可能误判为找回线

---

### 版本对比

| 特性 | v1.1.0 | v1.0.1 | v1.0.0 |
|------|:------:|:------:|:------:|
| 脱线恢复 | 陀螺仪辅助 | 稳定计数 | 保持/直走 |
| 方向判断 | 传感器+陀螺仪 | 传感器 | 传感器 |
| 抗转圈能力 | ✅ 强 | ⚠️ 弱 | ⚠️ 弱 |
| 独立脱线状态 | ✅ 有 | ✅ 有 | ❌ 无 |
| 恢复稳定计数 | — | ✅ 有 | ❌ 无 |
| 硬件需求 | +MPU6050 | 无 | 无 |
| 代码复杂度 | 高 | 中等 | 简单 |
| 当前状态 | ✅ 使用中 | 📦 归档 | 📦 归档 |

---

## 使用方法

1. 用 Keil 打开 `stm32standardtest.uvprojx`
2. 编译并烧录到 STM32F103C8
3. 小车上电后自动开始循迹
4. 如需调整效果，修改 `SPD_BASE`、`SPD_SLIGHT`、`SPD_STRONG` 参数重新编译

## MPU6050 接线（v1.1.0）

| MPU6050 | STM32 | 备注 |
|---------|-------|------|
| VCC | 3.3V | 不可接 5V |
| GND | GND | 共地 |
| SCL | PB8 | 需 4.7k 上拉到 3.3V |
| SDA | PB9 | 需 4.7k 上拉到 3.3V |
| AD0 | GND | I2C 地址 0x68 |

> 部分 MPU6050 模块（如 GY-521）板上已自带上拉电阻，无需外接。

## 待完善

- [ ] 启用串口实时监测（debug.c 已预留）
- [ ] 接入 PID 自适应控制（pid.c 已预留）
- [ ] 接入编码器闭环调速（encoder.c 已预留）
- [ ] 补充接线图

---

*STM32 学习记录，代码仅供参考，欢迎指正。*
